

{% extends 'base.html.twig' %}

{% block body %}
   <div class="jumbotron">
  <h1 class="display-4">{{ contact.nom }} {{ contact.prenom }}</h1>
  <p class="lead">Il habite au {{ contact.adresse}} - {{ contact.ville }}, et il a {{ contact.age}} ans.</p>
  <hr class="my-4">
  <p>Pour le contacter : {{contact.telephone}} </p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
</div>
{% endblock %}